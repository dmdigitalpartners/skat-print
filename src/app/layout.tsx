import type { Metadata } from 'next'
import { Montserrat, Inter } from 'next/font/google'
import { SITE_URL } from '@/config/site'
import './globals.css'

const montserrat = Montserrat({
  subsets: ['latin', 'cyrillic'],
  weight: ['600', '700', '800'],
  variable: '--font-display',
  display: 'swap',
})

const inter = Inter({
  subsets: ['latin', 'cyrillic'],
  weight: ['400', '500', '600'],
  variable: '--font-body',
  display: 'swap',
})

export const metadata: Metadata = {
  metadataBase: new URL(SITE_URL),
  title: {
    default: 'Skat Print — Custom Corrugated Packaging & POS Displays',
    template: '%s | Skat Print',
  },
  description:
    'Custom corrugated packaging and POS displays since 1995. EU-certified materials, fast turnaround, flexible MOQs. Bulgaria.',
  openGraph: {
    siteName: 'Skat Print',
    type: 'website',
    // OG image is generated by app/opengraph-image.tsx (Next.js file convention)
    // TODO: replace opengraph-image.tsx with a professionally designed asset
  },
  twitter: {
    card: 'summary_large_image',
  },
  icons: {
    icon: [
      { url: '/favicon/favicon-16x16.png', sizes: '16x16', type: 'image/png' },
      { url: '/favicon/favicon-32x32.png', sizes: '32x32', type: 'image/png' },
    ],
    apple: { url: '/favicon/apple-touch-icon.png', sizes: '180x180', type: 'image/png' },
  },
  manifest: '/manifest.json',
}

export default function RootLayout({ children }: { children: React.ReactNode }) {
  return (
    <html
      lang="bg"
      suppressHydrationWarning
      className={`${montserrat.variable} ${inter.variable} h-full antialiased`}
    >
      <head>
        {/* Resource hints for external dependencies actually loaded at runtime.
            (next/font/google self-hosts font files at build time — no runtime
            request to Google's font CDN, so no preconnect needed for it.) */}
        <link rel="dns-prefetch" href="https://maps.googleapis.com" />
        <link rel="dns-prefetch" href="https://www.googletagmanager.com" />
      </head>
      <body className="min-h-full flex flex-col bg-bg text-text">{children}</body>
    </html>
  )
}
